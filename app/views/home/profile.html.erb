<h1>Your profile</h1>

<table class="table table-striped table-bordered">
  <tr>
  <th>Book name</th>
  <th>Book Type</th>
  <th>Book on Loan?</th>
  </tr>
  <% @books = Book.userBooks(current_user) %>
  <% @books.each do |book| %>
  <tr>
    <td><%= book.name %></td>
    <td><%= book.booktype %></td>
    <td>unknown</td>
  </tr>
  <% end %>
<table>