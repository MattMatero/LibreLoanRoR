<table class="table table-striped table-bordered">
  <tr>
    <th>Book Name</th>
    <th>Borrower's Email</th>
  </tr>
  <% @loans.each do |loan| %>
  <tr>
    <td><%= loan.book_id %></td>
    <td><%= loan.borrow_id %></td>
  </tr>
  <% end %>
</table>